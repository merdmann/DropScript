<Page x:Name="pageRun" x:Class="UIDropScript.UIRun"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      
      xmlns:settings="clr-namespace:UIDropScript.Properties"
      xmlns:local="clr-namespace:UIDropScript"
        
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Execute">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:UIDropBox  Grid.Row="0" Grid.Column="0" AllowDrop="True"/>
        <local:UIDropBox  Grid.Row="0" Grid.Column="1" AllowDrop="True"/>
        <local:UIDropBox  Grid.Row="1" Grid.Column="0" AllowDrop="True"/>
        <local:UIDropBox  Grid.Row="1" Grid.Column="1" AllowDrop="True"/>
    </Grid>
</Page>
